require 'spec_helper'

describe RelatedWord do
  it 'has a version number' do
    expect(RelatedWord::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
